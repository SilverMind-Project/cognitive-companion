apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cc-backend-data
  labels:
    app: ai-api-gateway
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: microk8s-hostpath
