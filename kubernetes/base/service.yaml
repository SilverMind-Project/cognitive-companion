apiVersion: v1
kind: Service
metadata:
  name: ai-api-gateway-svc
  labels:
    app: ai-api-gateway
spec:
  type: ClusterIP
  selector:
    app: ai-api-gateway
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
